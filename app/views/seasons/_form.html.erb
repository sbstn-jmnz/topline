<%= form_with(model: season, local: true) do |form| %>
  <% if season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
      <% season.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :season_name %>
  </div>

  <div class="field">
    <%= form.label :closed %>
    <%= form.check_box :closed, id: :closed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
