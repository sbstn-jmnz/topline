<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :proforma %>
    <%= form.text_field :proforma, id: :order_proforma %>
  </div>

  <div class="field">
    <%= form.label :quotation_id %>
    <%= form.text_field :quotation_id, id: :order_quotation_id %>
  </div>

  <div class="field">
    <%= form.label :quotationdetail_id %>
    <%= form.text_field :quotationdetail_id, id: :order_quotationdetail_id %>
  </div>

  <div class="field">
    <%= form.label :variant_id %>
    <%= form.text_field :variant_id, id: :order_variant_id %>
  </div>

  <div class="field">
    <%= form.label :totalorder %>
    <%= form.text_field :totalorder, id: :order_totalorder %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
