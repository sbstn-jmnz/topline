<%= form_with(model: supplier, local: true) do |form| %>
  <% if supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
      <% supplier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :supplier_name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :supplier_address %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.collection_select(:country, Country.all, :country_code, :name,
    {:prompt => 'Select'})%></div>

  <div class="field">
    <%= form.label :contact %>
    <%= form.text_field :contact, id: :supplier_contact %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :supplier_phone %>
  </div>

  <div class="field">
    <%= form.label :fax %>
    <%= form.text_field :fax, id: :supplier_fax %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :supplier_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
