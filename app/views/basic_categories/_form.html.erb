<%= form_with(model: basic_category, local: true) do |form| %>
  <% if basic_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(basic_category.errors.count, "error") %> prohibited this basic_category from being saved:</h2>

      <ul>
      <% basic_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :basic_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
